using UnityEngine;

// This component is no longer needed - all pad interaction
// is handled by PlayerController and MartianSequencePuzzle
// Keep it for reference or delete it entirely

public class PadButton : MonoBehaviour
{
    // Deprecated: pad interaction now handled via PlayerController
    // and proximity detection in MartianSequencePuzzle
}